/**
 * @the-forge-flow/gitnexus-pi entry point.
 * Real implementation lands incrementally in subsequent tasks.
 */
export default function gitnexusExtension(): void {
	// Wiring is added in Task 13 once all modules exist.
}
